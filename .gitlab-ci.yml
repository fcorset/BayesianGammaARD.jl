compile_pdf:
  stage: build
  image: aergus/latex:latest  # use a Docker image for LaTeX from https://hub.docker.com/
  script: 
    - ls
    - latexmk -pdf main.tex  # build the pdf just as you would on your computer
  artifacts:
    paths: 
      - main.pdf  # instruct GitLab to keep the main.pdf file
