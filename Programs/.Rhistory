# on resimule un x depuis 0
x[nb.cycles,1] = 0     # initialisation du processus Gamma = 0 à t=0
for(i in 2:n){
x[nb.cycles,i] = x[nb.cycles,i-1] + rgamma(1,scale = b, shape = alpha*(temps[i]^beta)-temps[i-1]^beta)
}
id.newcycle <- FALSE
}
y[((j-1)*tau/pas+2):(j*(tau/pas)+1)]<-y[(j-1)*tau/pas+1]+ x[nb.cycles,((j-j.newcycle-1)*tau/pas+2):((j-j.newcycle)*(tau/pas)+1)]- x[nb.cycles,(j-j.newcycle-1)*tau/pas+1]
obs[j]<-y[j*tau/pas+1]
if (obs[j] < L) {
print("no action")
} else {
if (obs[j]<M) {
y[j*tau/pas+1]<-(1-rho)*y[j*tau/pas+1] # on écrase
} else {
y[j*tau/pas+1]<-0
id.newcycle <- TRUE
j.newcycle <- j
nb.cycles <- nb.cycles+1
}
}
j<-j+1
# boucle sur un cycle de renouvellement
if (id.newcycle){
# on resimule un x depuis 0
x[nb.cycles,1] = 0     # initialisation du processus Gamma = 0 à t=0
for(i in 2:n){
x[nb.cycles,i] = x[nb.cycles,i-1] + rgamma(1,scale = b, shape = alpha*(temps[i]^beta)-temps[i-1]^beta)
}
id.newcycle <- FALSE
}
y[((j-1)*tau/pas+2):(j*(tau/pas)+1)]<-y[(j-1)*tau/pas+1]+ x[nb.cycles,((j-j.newcycle-1)*tau/pas+2):((j-j.newcycle)*(tau/pas)+1)]- x[nb.cycles,(j-j.newcycle-1)*tau/pas+1]
obs[j]<-y[j*tau/pas+1]
if (obs[j] < L) {
print("no action")
} else {
if (obs[j]<M) {
y[j*tau/pas+1]<-(1-rho)*y[j*tau/pas+1] # on écrase
} else {
y[j*tau/pas+1]<-0
id.newcycle <- TRUE
j.newcycle <- j
nb.cycles <- nb.cycles+1
}
}
j<-j+1
obs
id.newcycle
# boucle sur un cycle de renouvellement
if (id.newcycle){
# on resimule un x depuis 0
x[nb.cycles,1] = 0     # initialisation du processus Gamma = 0 à t=0
for(i in 2:n){
x[nb.cycles,i] = x[nb.cycles,i-1] + rgamma(1,scale = b, shape = alpha*(temps[i]^beta)-temps[i-1]^beta)
}
id.newcycle <- FALSE
}
y[((j-1)*tau/pas+2):(j*(tau/pas)+1)]<-y[(j-1)*tau/pas+1]+ x[nb.cycles,((j-j.newcycle-1)*tau/pas+2):((j-j.newcycle)*(tau/pas)+1)]- x[nb.cycles,(j-j.newcycle-1)*tau/pas+1]
obs[j]<-y[j*tau/pas+1]
if (obs[j] < L) {
print("no action")
} else {
if (obs[j]<M) {
y[j*tau/pas+1]<-(1-rho)*y[j*tau/pas+1] # on écrase
} else {
y[j*tau/pas+1]<-0
id.newcycle <- TRUE
j.newcycle <- j
nb.cycles <- nb.cycles+1
}
}
j<-j+1
# boucle sur un cycle de renouvellement
if (id.newcycle){
# on resimule un x depuis 0
x[nb.cycles,1] = 0     # initialisation du processus Gamma = 0 à t=0
for(i in 2:n){
x[nb.cycles,i] = x[nb.cycles,i-1] + rgamma(1,scale = b, shape = alpha*(temps[i]^beta)-temps[i-1]^beta)
}
id.newcycle <- FALSE
}
y[((j-1)*tau/pas+2):(j*(tau/pas)+1)]<-y[(j-1)*tau/pas+1]+ x[nb.cycles,((j-j.newcycle-1)*tau/pas+2):((j-j.newcycle)*(tau/pas)+1)]- x[nb.cycles,(j-j.newcycle-1)*tau/pas+1]
obs[j]<-y[j*tau/pas+1]
if (obs[j] < L) {
print("no action")
} else {
if (obs[j]<M) {
y[j*tau/pas+1]<-(1-rho)*y[j*tau/pas+1] # on écrase
} else {
y[j*tau/pas+1]<-0
id.newcycle <- TRUE
j.newcycle <- j
nb.cycles <- nb.cycles+1
}
}
j<-j+1
# boucle sur un cycle de renouvellement
if (id.newcycle){
# on resimule un x depuis 0
x[nb.cycles,1] = 0     # initialisation du processus Gamma = 0 à t=0
for(i in 2:n){
x[nb.cycles,i] = x[nb.cycles,i-1] + rgamma(1,scale = b, shape = alpha*(temps[i]^beta)-temps[i-1]^beta)
}
id.newcycle <- FALSE
}
y[((j-1)*tau/pas+2):(j*(tau/pas)+1)]<-y[(j-1)*tau/pas+1]+ x[nb.cycles,((j-j.newcycle-1)*tau/pas+2):((j-j.newcycle)*(tau/pas)+1)]- x[nb.cycles,(j-j.newcycle-1)*tau/pas+1]
obs[j]<-y[j*tau/pas+1]
if (obs[j] < L) {
print("no action")
} else {
if (obs[j]<M) {
y[j*tau/pas+1]<-(1-rho)*y[j*tau/pas+1] # on écrase
} else {
y[j*tau/pas+1]<-0
id.newcycle <- TRUE
j.newcycle <- j
nb.cycles <- nb.cycles+1
}
}
j<-j+1
obs
# boucle sur un cycle de renouvellement
if (id.newcycle){
# on resimule un x depuis 0
x[nb.cycles,1] = 0     # initialisation du processus Gamma = 0 à t=0
for(i in 2:n){
x[nb.cycles,i] = x[nb.cycles,i-1] + rgamma(1,scale = b, shape = alpha*(temps[i]^beta)-temps[i-1]^beta)
}
id.newcycle <- FALSE
}
y[((j-1)*tau/pas+2):(j*(tau/pas)+1)]<-y[(j-1)*tau/pas+1]+ x[nb.cycles,((j-j.newcycle-1)*tau/pas+2):((j-j.newcycle)*(tau/pas)+1)]- x[nb.cycles,(j-j.newcycle-1)*tau/pas+1]
obs[j]<-y[j*tau/pas+1]
if (obs[j] < L) {
print("no action")
} else {
if (obs[j]<M) {
y[j*tau/pas+1]<-(1-rho)*y[j*tau/pas+1] # on écrase
} else {
y[j*tau/pas+1]<-0
id.newcycle <- TRUE
j.newcycle <- j
nb.cycles <- nb.cycles+1
}
}
j<-j+1
# boucle sur un cycle de renouvellement
if (id.newcycle){
# on resimule un x depuis 0
x[nb.cycles,1] = 0     # initialisation du processus Gamma = 0 à t=0
for(i in 2:n){
x[nb.cycles,i] = x[nb.cycles,i-1] + rgamma(1,scale = b, shape = alpha*(temps[i]^beta)-temps[i-1]^beta)
}
id.newcycle <- FALSE
}
y[((j-1)*tau/pas+2):(j*(tau/pas)+1)]<-y[(j-1)*tau/pas+1]+ x[nb.cycles,((j-j.newcycle-1)*tau/pas+2):((j-j.newcycle)*(tau/pas)+1)]- x[nb.cycles,(j-j.newcycle-1)*tau/pas+1]
obs[j]<-y[j*tau/pas+1]
if (obs[j] < L) {
print("no action")
} else {
if (obs[j]<M) {
y[j*tau/pas+1]<-(1-rho)*y[j*tau/pas+1] # on écrase
} else {
y[j*tau/pas+1]<-0
id.newcycle <- TRUE
j.newcycle <- j
nb.cycles <- nb.cycles+1
}
}
j<-j+1
obs
# boucle sur un cycle de renouvellement
if (id.newcycle){
# on resimule un x depuis 0
x[nb.cycles,1] = 0     # initialisation du processus Gamma = 0 à t=0
for(i in 2:n){
x[nb.cycles,i] = x[nb.cycles,i-1] + rgamma(1,scale = b, shape = alpha*(temps[i]^beta)-temps[i-1]^beta)
}
id.newcycle <- FALSE
}
y[((j-1)*tau/pas+2):(j*(tau/pas)+1)]<-y[(j-1)*tau/pas+1]+ x[nb.cycles,((j-j.newcycle-1)*tau/pas+2):((j-j.newcycle)*(tau/pas)+1)]- x[nb.cycles,(j-j.newcycle-1)*tau/pas+1]
obs[j]<-y[j*tau/pas+1]
if (obs[j] < L) {
print("no action")
} else {
if (obs[j]<M) {
y[j*tau/pas+1]<-(1-rho)*y[j*tau/pas+1] # on écrase
} else {
y[j*tau/pas+1]<-0
id.newcycle <- TRUE
j.newcycle <- j
nb.cycles <- nb.cycles+1
}
}
j<-j+1
obs
# boucle sur un cycle de renouvellement
if (id.newcycle){
# on resimule un x depuis 0
x[nb.cycles,1] = 0     # initialisation du processus Gamma = 0 à t=0
for(i in 2:n){
x[nb.cycles,i] = x[nb.cycles,i-1] + rgamma(1,scale = b, shape = alpha*(temps[i]^beta)-temps[i-1]^beta)
}
id.newcycle <- FALSE
}
y[((j-1)*tau/pas+2):(j*(tau/pas)+1)]<-y[(j-1)*tau/pas+1]+ x[nb.cycles,((j-j.newcycle-1)*tau/pas+2):((j-j.newcycle)*(tau/pas)+1)]- x[nb.cycles,(j-j.newcycle-1)*tau/pas+1]
obs[j]<-y[j*tau/pas+1]
if (obs[j] < L) {
print("no action")
} else {
if (obs[j]<M) {
y[j*tau/pas+1]<-(1-rho)*y[j*tau/pas+1] # on écrase
} else {
y[j*tau/pas+1]<-0
id.newcycle <- TRUE
j.newcycle <- j
nb.cycles <- nb.cycles+1
}
}
j<-j+1
# boucle sur un cycle de renouvellement
if (id.newcycle){
# on resimule un x depuis 0
x[nb.cycles,1] = 0     # initialisation du processus Gamma = 0 à t=0
for(i in 2:n){
x[nb.cycles,i] = x[nb.cycles,i-1] + rgamma(1,scale = b, shape = alpha*(temps[i]^beta)-temps[i-1]^beta)
}
id.newcycle <- FALSE
}
y[((j-1)*tau/pas+2):(j*(tau/pas)+1)]<-y[(j-1)*tau/pas+1]+ x[nb.cycles,((j-j.newcycle-1)*tau/pas+2):((j-j.newcycle)*(tau/pas)+1)]- x[nb.cycles,(j-j.newcycle-1)*tau/pas+1]
obs[j]<-y[j*tau/pas+1]
if (obs[j] < L) {
print("no action")
} else {
if (obs[j]<M) {
y[j*tau/pas+1]<-(1-rho)*y[j*tau/pas+1] # on écrase
} else {
y[j*tau/pas+1]<-0
id.newcycle <- TRUE
j.newcycle <- j
nb.cycles <- nb.cycles+1
}
}
j<-j+1
obs
j
nb.inspections
id.newcycle
nb.cycles
# boucle sur un cycle de renouvellement
if (id.newcycle){
# on resimule un x depuis 0
x[nb.cycles,1] = 0     # initialisation du processus Gamma = 0 à t=0
for(i in 2:n){
x[nb.cycles,i] = x[nb.cycles,i-1] + rgamma(1,scale = b, shape = alpha*(temps[i]^beta)-temps[i-1]^beta)
}
id.newcycle <- FALSE
}
x[nb.cycles,1] = 0
nrow(x)<-nb.cycles
nrow(x)
rm(list=ls())
library(ggplot2)
library(tidyverse)
tau = 0.5 # intervalle inter-inspection
rho = 0.5 # parametre ARDinf
L=3 # seuil pour MP
M=6 # seuil pout MC
tps.final <-8 # fenêtre d'observation du processus
id.newcycle <- FALSE # Initialisation du nb de cycle de renouvellement
# Simulation d'un processus gamma jusqu'au temps final
pas = 0.01 # pas de temps pour simuler le processus
alpha = 1 # paramètre de forme de Gamma a = alpha (t)^beta
beta = 1.5 # paramètre de forme  de Gamma
b=1   # paramètre d'échelle du Gamma
temps = seq(from = 0,to = tps.final,by = pas)
n=length(temps)
nb.inspections<- floor(tps.final/tau) # nombre d'inspections max pendant la fenêtre d'observation.
obs<-numeric(nb.inspections) # données observées (pendant les inspections)
j<-1 # indicateur prochaine inspection
j.newcycle <- 0 # identifier le j où nouveau cycle
nb.cycles <- 1 # compteur de cycles
x=matrix(nrow=nb.inspections,ncol = n) # processus Gamma simulé
x[nb.cycles,1] = 0     # initialisation du processus Gamma = 0 à t=0
for(i in 2:n){
x[nb.cycles,i] = x[nb.cycles,i-1] + rgamma(1,scale = b, shape = alpha*(temps[i]^beta)-temps[i-1]^beta)
}
y=numeric(n) # processus Gamma maintenu
y[1]<-0
while (j<=nb.inspections) {
# boucle sur un cycle de renouvellement
if (id.newcycle){
# on resimule un x depuis 0
x[nb.cycles,1] = 0     # initialisation du processus Gamma = 0 à t=0
for(i in 2:n){
x[nb.cycles,i] = x[nb.cycles,i-1] + rgamma(1,scale = b, shape = alpha*(temps[i]^beta)-temps[i-1]^beta)
}
id.newcycle <- FALSE
}
y[((j-1)*tau/pas+2):(j*(tau/pas)+1)]<-y[(j-1)*tau/pas+1]+ x[nb.cycles,((j-j.newcycle-1)*tau/pas+2):((j-j.newcycle)*(tau/pas)+1)]- x[nb.cycles,(j-j.newcycle-1)*tau/pas+1]
obs[j]<-y[j*tau/pas+1]
if (obs[j] < L) {
print("no action")
} else {
if (obs[j]<M) {
y[j*tau/pas+1]<-(1-rho)*y[j*tau/pas+1] # on écrase
} else {
y[j*tau/pas+1]<-0
id.newcycle <- TRUE
j.newcycle <- j
nb.cycles <- nb.cycles+1
}
}
j<-j+1
}
obs
nb.cycles
plot(temps,x[1,],type="l")
plot(temps,x[2,],type="l")
plot(temps,y,type="l",ylim=c(0,max(x)),xlim = c(0,8))
plot(temps,x,col="red",type="l",ylim = c(0,max(x)),xlim = c(0,8))
plot(temps,x[1,],col="red",type="l",ylim = c(0,max(x)),xlim = c(0,8))
plot(temps,x[1,],col="red",type="l",ylim = c(0,max(x[1,])),xlim = c(0,8))
par(new=T)
plot(temps,y,type="l",ylim=c(0,max(x)),xlim = c(0,8))
par(new=T)
plot(temps,y,type="l",ylim=c(0,max(x[1,])),xlim = c(0,8))
plot(tau*(1:nb.inspection),numeric(length = nb.inspection),ylim=c(0,max(x)),type="p",xlim = c(0,8))
plot(tau*(1:nb.inspections),numeric(length = nb.inspections),ylim=c(0,max(x[1,])),type="p",xlim = c(0,8))
plot(temps,x[1,],col="red",type="l",ylim = c(0,max(x[1,])),xlim = c(0,8))
par(new=T)
plot(temps,y,type="l",ylim=c(0,max(x[1,])),xlim = c(0,8))
par(new=T)
plot(tau*(1:nb.inspections),numeric(length = nb.inspections),ylim=c(0,max(x[1,])),type="p",xlim = c(0,8))
abline(h=L,col="blue")
abline(h=M,col="red")
plot(temps,x[1,],col="red",type="l",ylim = c(0,max(x[1,])),xlim = c(0,8))
par(new=T)
plot(temps,x[2,],col="green",type="l",ylim = c(0,max(x[1,])),xlim = c(0,8))
par(new=T)
plot(temps,y,type="l",ylim=c(0,max(x[1,])),xlim = c(0,8))
par(new=T)
plot(tau*(1:nb.inspections),numeric(length = nb.inspections),ylim=c(0,max(x[1,])),type="p",xlim = c(0,8))
abline(h=L,col="blue")
abline(h=M,col="red")
rm(list=ls())
library(ggplot2)
library(tidyverse)
tau = 0.5 # intervalle inter-inspection
rho = 0.5 # parametre ARDinf
L=3 # seuil pour MP
M=6 # seuil pout MC
tps.final <-8 # fenêtre d'observation du processus
id.newcycle <- FALSE # Initialisation du nb de cycle de renouvellement
# Simulation d'un processus gamma jusqu'au temps final
pas = 0.01 # pas de temps pour simuler le processus
alpha = 1 # paramètre de forme de Gamma a = alpha (t)^beta
beta = 1.5 # paramètre de forme  de Gamma
b=1   # paramètre d'échelle du Gamma
temps = seq(from = 0,to = tps.final,by = pas)
n=length(temps)
nb.inspections<- floor(tps.final/tau) # nombre d'inspections max pendant la fenêtre d'observation.
obs<-numeric(nb.inspections) # données observées (pendant les inspections)
j<-1 # indicateur prochaine inspection
j.newcycle <- 0 # identifier le j où nouveau cycle
nb.cycles <- 1 # compteur de cycles
x=matrix(nrow=nb.inspections,ncol = n) # processus Gamma simulé, nb.lignes = nb.cycles
x[nb.cycles,1] = 0     # initialisation du processus Gamma = 0 à t=0
for(i in 2:n){
x[nb.cycles,i] = x[nb.cycles,i-1] + rgamma(1,scale = b, shape = alpha*(temps[i]^beta)-temps[i-1]^beta)
}
y=numeric(n) # processus Gamma maintenu
y[1]<-0
while (j<=nb.inspections) {
# boucle sur un cycle de renouvellement
if (id.newcycle){
# on resimule un x depuis 0
x[nb.cycles,1] = 0     # initialisation du processus Gamma = 0 à t=0
for(i in 2:n){
x[nb.cycles,i] = x[nb.cycles,i-1] + rgamma(1,scale = b, shape = alpha*(temps[i]^beta)-temps[i-1]^beta)
}
id.newcycle <- FALSE
}
y[((j-1)*tau/pas+2):(j*(tau/pas)+1)]<-y[(j-1)*tau/pas+1]+ x[nb.cycles,((j-j.newcycle-1)*tau/pas+2):((j-j.newcycle)*(tau/pas)+1)]- x[nb.cycles,(j-j.newcycle-1)*tau/pas+1]
obs[j]<-y[j*tau/pas+1] # état dégradation à l'inspection (t_j^-)
if (obs[j] < L) {
print("no action")
} else {
if (obs[j]<M) {
y[j*tau/pas+1]<-(1-rho)*y[j*tau/pas+1] # on écrase (ARD infini)
} else {
y[j*tau/pas+1]<-0
id.newcycle <- TRUE
j.newcycle <- j
nb.cycles <- nb.cycles+1
}
}
j<-j+1
}
nb.cycles
plot(temps,x[1,],type="l")
plot(temps,x[2,],type="l")
df <- data.frame(temps,x,y)
plot(temps,x[1,],col="red",type="l",ylim = c(0,max(x[1,])),xlim = c(0,8))
par(new=T)
plot(temps,x[2,],col="green",type="l",ylim = c(0,max(x[1,])),xlim = c(0,8))
par(new=T)
plot(temps,y,type="l",ylim=c(0,max(x[1,])),xlim = c(0,8))
par(new=T)
plot(tau*(1:nb.inspections),numeric(length = nb.inspections),ylim=c(0,max(x[1,])),type="p",xlim = c(0,8))
abline(h=L,col="blue")
abline(h=M,col="red")
obs
j.newcycle
nb.inspections
tau/pas+1*(&:16)
(tau/pas+1)*(1:16)
temps[(tau/pas+1)*(1:16)]
(1:nb.inspections)*tau
attach(Hands)
source('~/Documents/Recherche/degradation/Programs/simu_model1.R')
# Simulation d'un processus gamma jusqu'au temps final
pas = 0.01 # pas de temps pour simuler le processus
source('~/Documents/Recherche/degradation/Programs/simu_model1.R')
source('~/Documents/Recherche/degradation/Programs/simu_model1.R')
source('~/Documents/Recherche/degradation/Programs/simu_modele2.R')
temps.cycle<-c(0,tau*which(obs.av>M),tps.final)
indice.temps.cycle <- c(1,tau/pas*(which(obs.av>M))+1,length(temps))
for (k in 1:nb.cycles){
plot(temps[indice.temps.cycle[k]:indice.temps.cycle[k+1]],x[k,1:(indice.temps.cycle[k+1]-indice.temps.cycle[k]+1)],col="red",type="l",ylim = c(0,2*M),xlim = c(0,tps.final),xlab="",ylab="")
par(new=T)
}
plot(temps,y,type="l",ylim=c(0,2*M),xlim = c(0,tps.final),ylab="",xlab="")
par(new=T)
plot(tau*(1:nb.inspections),numeric(length = nb.inspections),ylim=c(0,2*M),type="p",xlim = c(0,tps.final),xlab = "temps",ylab="Dégradation")
abline(h=L,col="blue")
abline(h=M,col="red")
abline(v=tau*(1:nb.inspections), lty =2)
abline(v=tau*(1:nb.inspections), lty =2, col = grey(0.2))
abline(v=tau*(1:nb.inspections), lty =2, col = grey(0.2))
for (k in 1:nb.cycles){
plot(temps[indice.temps.cycle[k]:indice.temps.cycle[k+1]],x[k,1:(indice.temps.cycle[k+1]-indice.temps.cycle[k]+1)],col="red",type="l",ylim = c(0,2*M),xlim = c(0,tps.final),xlab="",ylab="")
par(new=T)
}
plot(temps,y,type="l",ylim=c(0,2*M),xlim = c(0,tps.final),ylab="",xlab="")
par(new=T)
plot(tau*(1:nb.inspections),numeric(length = nb.inspections),ylim=c(0,2*M),type="p",xlim = c(0,tps.final),xlab = "temps",ylab="Dégradation")
abline(h=L,col="blue")
abline(h=M,col="red")
abline(v=tau*(1:nb.inspections), lty =2, col = grey(0.2))
for (k in 1:nb.cycles){
plot(temps[indice.temps.cycle[k]:indice.temps.cycle[k+1]],x[k,1:(indice.temps.cycle[k+1]-indice.temps.cycle[k]+1)],col="red",type="l",ylim = c(0,2*M),xlim = c(0,tps.final),xlab="",ylab="")
par(new=T)
}
plot(temps,y,type="l",ylim=c(0,2*M),xlim = c(0,tps.final),ylab="",xlab="")
par(new=T)
plot(tau*(1:nb.inspections),numeric(length = nb.inspections),ylim=c(0,2*M),type="p",xlim = c(0,tps.final),xlab = "temps",ylab="Dégradation")
abline(h=L,col="blue")
abline(h=M,col="red")
abline(v=tau*(1:nb.inspections), lty =3, col = grey(0.1))
for (k in 1:nb.cycles){
plot(temps[indice.temps.cycle[k]:indice.temps.cycle[k+1]],x[k,1:(indice.temps.cycle[k+1]-indice.temps.cycle[k]+1)],col="red",type="l",ylim = c(0,2*M),xlim = c(0,tps.final),xlab="",ylab="")
par(new=T)
}
plot(temps,y,type="l",ylim=c(0,2*M),xlim = c(0,tps.final),ylab="",xlab="")
par(new=T)
plot(tau*(1:nb.inspections),numeric(length = nb.inspections),ylim=c(0,2*M),type="p",xlim = c(0,tps.final),xlab = "temps",ylab="Dégradation")
abline(h=L,col="blue")
abline(h=M,col="red")
abline(v=tau*(1:nb.inspections), lty =3, col = grey(0.01))
for (k in 1:nb.cycles){
plot(temps[indice.temps.cycle[k]:indice.temps.cycle[k+1]],x[k,1:(indice.temps.cycle[k+1]-indice.temps.cycle[k]+1)],col="red",type="l",ylim = c(0,2*M),xlim = c(0,tps.final),xlab="",ylab="")
par(new=T)
}
plot(temps,y,type="l",ylim=c(0,2*M),xlim = c(0,tps.final),ylab="",xlab="")
par(new=T)
plot(tau*(1:nb.inspections),numeric(length = nb.inspections),ylim=c(0,2*M),type="p",xlim = c(0,tps.final),xlab = "temps",ylab="Dégradation")
abline(h=L,col="blue")
abline(h=M,col="red")
abline(v=tau*(1:nb.inspections), lty =3, col = grey(0.001))
for (k in 1:nb.cycles){
plot(temps[indice.temps.cycle[k]:indice.temps.cycle[k+1]],x[k,1:(indice.temps.cycle[k+1]-indice.temps.cycle[k]+1)],col="red",type="l",ylim = c(0,2*M),xlim = c(0,tps.final),xlab="",ylab="")
par(new=T)
}
plot(temps,y,type="l",ylim=c(0,2*M),xlim = c(0,tps.final),ylab="",xlab="")
par(new=T)
plot(tau*(1:nb.inspections),numeric(length = nb.inspections),ylim=c(0,2*M),type="p",xlim = c(0,tps.final),xlab = "temps",ylab="Dégradation")
abline(h=L,col="blue")
abline(h=M,col="red")
abline(v=tau*(1:nb.inspections), lty =3, col = grey(0.001), lwd = 0.5)
for (k in 1:nb.cycles){
plot(temps[indice.temps.cycle[k]:indice.temps.cycle[k+1]],x[k,1:(indice.temps.cycle[k+1]-indice.temps.cycle[k]+1)],col="red",type="l",ylim = c(0,2*M),xlim = c(0,tps.final),xlab="",ylab="")
par(new=T)
}
plot(temps,y,type="l",ylim=c(0,2*M),xlim = c(0,tps.final),ylab="",xlab="")
par(new=T)
plot(tau*(1:nb.inspections),numeric(length = nb.inspections),ylim=c(0,2*M),type="p",xlim = c(0,tps.final),xlab = "temps",ylab="Dégradation")
abline(h=L,col="blue")
abline(h=M,col="red")
abline(v=tau*(1:nb.inspections), lty =3, col = grey(0.001), lwd = 0.25)
grey(0.001)
grey(0.01)
for (k in 1:nb.cycles){
plot(temps[indice.temps.cycle[k]:indice.temps.cycle[k+1]],x[k,1:(indice.temps.cycle[k+1]-indice.temps.cycle[k]+1)],col="red",type="l",ylim = c(0,2*M),xlim = c(0,tps.final),xlab="",ylab="")
par(new=T)
}
plot(temps,y,type="l",ylim=c(0,2*M),xlim = c(0,tps.final),ylab="",xlab="")
par(new=T)
plot(tau*(1:nb.inspections),numeric(length = nb.inspections),ylim=c(0,2*M),type="p",xlim = c(0,tps.final),xlab = "temps",ylab="Dégradation")
abline(h=L,col="blue")
abline(h=M,col="red")
abline(v=tau*(1:nb.inspections), lty =3, col = grey(0.1), lwd = 0.45)
source('~/Documents/Recherche/degradation/Programs/simu_model1.R')
