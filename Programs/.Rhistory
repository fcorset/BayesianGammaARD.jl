library(ggplot2)
library(tidyverse)
source('~/Documents/bureau/Papiers/encours/AnalyseSensibilite/degradation/Programs/simu.R')
grid_abs <- seq(0:8,0.1)
grid_abs <- seq(0,8,0.1)
for(k in 1:10){}
w<-matrix(nrow = K,ncol = length(grid_abs))
K<-10
grid_abs <- seq(0,8,0.1)
w<-matrix(nrow = K,ncol = length(grid_abs))
w
w[1,]<-dexp(grid_abs,rate = 1)
w[1,]
grid_abs
dexp(grid_abs,rate = 1)
dexp
?dexp
w[1,]<-grid_abd*dexp(grid_abs,rate = 1)
w[1,]<-grid_abs*dexp(grid_abs,rate = 1)
w[1,]
list(dexp,rexp)
list(dexp(),rexp())
list(dexp(x),rexp(x))
list(dexp(x),rexp(x))->test
test
test[[1]]
x
pi<-list()
pi
pi[[1]]<-function(x) dexp(x)
pi
pi[[1]](1)
pi[[1]](0)
pi[[1]](10)
pi[[1]]<-function(x) x*dexp(x)
pi[[1]](10)
pi[[1]](0)
w[1,]<-pi[[1]](grid_abs)
w[1,]
f<-function(x) beta^(alpha*tau)/Gamma(alpha*tau)*x^(alpha*tau-1)*exp(-beta*x)
?dgamma
dgamma(rate=beta,shape=alpha*tau)
curve(dgamma(x,rate=beta,shape=alpha*tau))
dgamma(0,rate=beta,shape=alpha*tau)
alpha
alpha = 1 # paramètre de forme de Gamma a = alpha t
beta = 1 # paramètre d'échelle de Gamma
dgamma(0,rate=beta,shape=alpha*tau)
tau
?min
k<-2
for (abs in grid_abs){
w[2,abs]<-integrate(function(x) pi[k-1](x)*dgamma(abs-x),0,min(c(L,abs))) + integrate(function(x) pi[k-1](x)*dgamma(abs-(1-rho)*x),min(c(L,abs/(1-rho))),min(c(M,abs/(1-rho))))
}
for (abs in grid_abs){
w[2,abs]<-integrate(function(x) pi[[k-1]](x)*dgamma(abs-x),0,min(c(L,abs))) + integrate(function(x) pi[[k-1]](x)*dgamma(abs-(1-rho)*x),min(c(L,abs/(1-rho))),min(c(M,abs/(1-rho))))
}
for (abs in grid_abs){
w[2,abs]<-integrate(function(x) pi[[k-1]](x)*dgamma(abs-x),0,min(c(L,abs))) + integrate(function(x) pi[[k-1]](x)*dgamma(abs-(1-rho)*x),min(c(L,abs/(1-rho))),min(c(M,abs/(1-rho))))
}
for (abs in grid_abs){
w[2,abs]<-integrate(function(x) pi[[k-1]](x)*dgamma(abs-x,rate=beta,shape=alpha*tau),0,min(c(L,abs))) + integrate(function(x) pi[[k-1]](x)*dgamma(abs-(1-rho)*x,rate=beta,shape=alpha*tau),min(c(L,abs/(1-rho))),min(c(M,abs/(1-rho))))
}
for (abs in grid_abs){
w[2,abs]<-integrate(function(x) pi[[k-1]](x)*dgamma(abs-x,rate=beta,shape=alpha*tau),0,min(c(L,abs))) + integrate(function(x) pi[[k-1]](x)*dgamma(abs-(1-rho)*x,rate=beta,shape=alpha*tau),min(c(L,abs/(1-rho))),min(c(M,abs/(1-rho))))
}
min(c(L,abs))
L
abs
dgamma(abs-x,rate=beta,shape=alpha*tau)
K<-10 # nb itérations de l'algo de point fixe
grid_abs <- seq(0.01,8,0.01)
w<-matrix(nrow = K,ncol = length(grid_abs))
pi<-list()
pi[[1]]<-function(x) x*dexp(x)
w[1,]<-pi[[1]](grid_abs)
for (abs in grid_abs){
w[2,abs]<-integrate(function(x) pi[[k-1]](x)*dgamma(abs-x,rate=beta,shape=alpha*tau),0,min(c(L,abs))) + integrate(function(x) pi[[k-1]](x)*dgamma(abs-(1-rho)*x,rate=beta,shape=alpha*tau),min(c(L,abs/(1-rho))),min(c(M,abs/(1-rho))))
}
for (abs in grid_abs){
w[2,abs]<-integrate(function(x) pi[[k-1]](x)*dgamma(abs-x,rate=beta,shape=alpha*tau),0,min(c(L,abs))) + ifelse(abs<(1-rho)*L,0,integrate(function(x) pi[[k-1]](x)*dgamma(abs-(1-rho)*x,rate=beta,shape=alpha*tau),L,min(c(M,abs/(1-rho)))))
}
